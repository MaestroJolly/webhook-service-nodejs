{
  "name": "webhook-service-nodejs",
  "version": "1.0.0",
  "description": "Webhook application built in nodeJS with slack hook notification support for receiving events. This can also support database implementation for hook data storage if preferred.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js",
    "start:dev": "nodemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "request-ip": "^2.1.3",
    "winston": "^3.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.9"
  }
}
